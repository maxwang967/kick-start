class Solution:
    def printNumbers(self, n: int) -> List[int]:
        def dfs(x):
            if x == n:
                pass
            for i in range(10):
                pass
        # return list(range(1, 10 ** n))
        
        result = []
        self.start = n - 1
        self.nine = 0